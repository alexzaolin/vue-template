import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

const state = {
}

const mutations = {
}

/*状态保存在 store 中，状态是只读的，只能通过 action 触发 store 的更新*/
const actions = {
}

const getters = {
}

export default new Vuex.Store({
    state ,
    mutations ,
    actions ,
    getters
})
